{% extends "layout.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
<div class="container-fluid image">
    		<h1>Billet simple pour l'Alaska 
    			<small> de Jean Forteroche</small>
    		</h1>
            <a href="#article"><button class="btn btn-custom">Articles</button></a>

</div>
<div class=" container title">
	<h1>Articles</h1>
</div>

<article>
{% for article in articles %}
<div class="container" id="article">
    <h2><a class="articleTitle" href="{{ path('article', { 'id': article.id }) }}">{{ article.title }}</a></h2>

    <p>{{ article.content | raw}}</p>
    <em>Ajouté le {{ article.date }}</em>
</div>
</article>
{% endfor %}
{% endblock %}